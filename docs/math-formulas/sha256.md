# SHA-256

## Mathematical Definition

Merkle-Damgård hash with 256-bit output:

$$H(M) = \text{SHA256}(M) \in \{0,1\}^{256}$$

One-way function; no decryption defined.

## Dataset

`datasets/fingerprinted/sha256/data.jsonl` (encrypt_only)
